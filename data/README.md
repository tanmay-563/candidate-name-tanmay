# Data Directory

This directory contains the sample input files used in the repository documentation, CLI example, and integration tests.

Included files:

- `recruiter.csv`: a recruiter-provided CSV export containing contact and current-role details
- `github.json`: a GitHub profile JSON document containing public profile metadata and language-derived skills

These files are intentionally small and realistic so the end-to-end pipeline can be demonstrated quickly during review.
