# Data Directory

Reserved for source fixtures, local experimentation datasets, and future transformation outputs.

Suggested future layout:

- `raw/`: input payloads exactly as received from upstream systems
- `interim/`: parsed or partially normalized artifacts used during development
- `processed/`: validated outputs intended for downstream consumption
