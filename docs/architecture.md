# Architecture Notes

The submission-ready architecture summary lives in the repository root at [ARCHITECTURE.md](../ARCHITECTURE.md).

This file is kept as a stable docs entrypoint for readers who start in the `docs/` directory.
